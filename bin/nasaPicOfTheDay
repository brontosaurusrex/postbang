#!/bin/bash

# feh needed, since it can download http files directly

test=$(curl -s http://www.nasa.gov/rss/dyn/image_of_the_day.rss | grep -m1 "<enclosure" | sed 's/^.*url="//' | sed 's/" len.*$//') && feh -. "$test"



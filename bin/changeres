#!/bin/bash

# fixme: tmp file is not needed, use variable

if dmesg | grep -i Virtualbox ; then

	while true ; do
	
	diff /tmp/xrandr.txt <(xrandr) || nitrogen --restore
	
	xrandr > /tmp/xrandr.txt
	
	sleep 5; done

else

	exit
     
fi
